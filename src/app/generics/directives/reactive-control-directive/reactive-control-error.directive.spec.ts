import { ReactiveControlErrorDirective } from './reactive-control-error.directive';

describe('ReactiveControlErrorDirective', () => {
	it('should create an instance', () => {
		const directive = new ReactiveControlErrorDirective();
		expect(directive).toBeTruthy();
	});
});
